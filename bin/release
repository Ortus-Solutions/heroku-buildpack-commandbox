#!/usr/bin/env bash
# bin/release <build-dir>

echo "---"

cat << EOF
addons: []
default_process_types:
  web: "cd /app && ./bin/box server set web.http.port=\$PORT && ./bin/box server start && sleep 5 && box server log --follow"
EOF